# 数览 Sloth — 安全指南

## 🔐 Supabase 安全配置

### 1. RLS 策略（已配置）
- ✅ 用户只能查看自己的文件
- ✅ 用户只能插入自己的文件
- ✅ 用户只能更新自己的文件
- ✅ 用户只能删除自己的文件
- ✅ Storage 策略按用户 ID 隔离

### 2. 认证安全
- JWT Token 自动管理
- 登录状态通过 Session 维护
- 建议：生产环境启用 Email 确认

## 📁 文件安全

### 1. 文件隔离
- 存储路径：`{user_id}/{timestamp}_{filename}`
- 每个用户只能访问自己的目录

### 2. 文件大小限制
- 单个文件最大 20MB
- 上传时自动检查

### 3. 文件类型
- 建议在上传时验证文件扩展名
- 只允许 Excel/CSV 格式

## 🛡️ 前端安全

### 1. 环境变量
- Supabase URL 和 Key 在 `supabase-config.js`
- 该文件已添加到 `.gitignore`

### 2. XSS 防护
- 用户输入已转义（escapeHtml 函数）
- 文件名已转义（escapeAttr 函数）

### 3. 点击劫持防护
- 所有交互按钮使用 `event.stopPropagation()`

## ✅ 安全检查清单

| 检查项 | 状态 | 说明 |
|--------|------|------|
| RLS 策略 | ✅ | 4 条策略覆盖 CRUD |
| 文件隔离 | ✅ | 按用户 ID 分目录 |
| JWT Token | ✅ | Supabase 自动管理 |
| 环境变量分离 | ✅ | .gitignore 已配置 |
| 文件大小限制 | ✅ | 20MB 限制 |
| XSS 防护 | ✅ | 输入已转义 |
| HTTPS | ⚠️ | 生产环境必须启用 |
| Email 确认 | ⚠️ | 建议启用 |

## 🚀 部署建议

1. 确保使用 HTTPS
2. 启用 Supabase Email 确认
3. 定期审查 RLS 策略
4. 监控异常登录
5. 定期备份数据库

## 📞 安全问题

如发现安全漏洞，请勿公开报告，直接联系项目维护者。
